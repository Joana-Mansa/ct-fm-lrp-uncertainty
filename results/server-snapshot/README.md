# Server records at the start of the audit

These files preserve the initial `iv-mind2` checkout before reconciling newer GitHub records. The seed-1 file is partial; both files differ from the subsequently published completed records at some label budgets. Their exact relationship cannot be established from the saved metadata alone. They are not additional independent seeds.

The top-level `results/ablation_seed1.json` and `ablation_seed2.json` contain the completed records retrieved from GitHub commit `0f9d732`. The reader-facing paired summary uses those completed records. Checkpoint re-evaluation is reported separately. This archive preserves provenance rather than silently replacing conflicting measurements.
